# Clean demo app

Example Node.js project with normal docs and config.

## Setup

```bash
cp .env.example .env
npm install
npm start
```

Configure your API key in `.env`. Do not commit real secrets.

Webhook docs sometimes mention `https://example.com/api/webhooks/test` for local testing.
